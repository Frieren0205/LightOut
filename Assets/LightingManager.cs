using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LightingManager : MonoBehaviour
{

    public Texture2D lightmap1_A1;
    public Texture2D lightmap1_A2;
    public Texture2D lightmap2_A1;
    public Texture2D lightmap2_A2;
    public LightmapData[] lightmapDatas = new LightmapData[5];

    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
